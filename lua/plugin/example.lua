return {
    -- 插件配置示例

    -- 'repo/plugin',
    -- main = '',
    -- branch = '',
    -- lazy = true,
    -- event = 'VeryLazy',
    -- dependencies = {}
    -- build = 'commands',
    -- cmd = 'commands',
    -- init = function () end,
    -- opts = {},
    -- config = function (_, opts) end,
    -- keys = {}
    -- deactivate = function () end
}
